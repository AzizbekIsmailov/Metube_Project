spring:
  application:
   name: VIDEO-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/metube_project
    username: postgres
    password: 2907
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
